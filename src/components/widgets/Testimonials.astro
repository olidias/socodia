---
export interface Props {
  title?: string;
  highlight?: string;
  description?: string;
  testimonials: Array<Testimonial>;
}
export interface Testimonial {
  title?: string;
  author: string;
  description: string;
  url?: string;
  coverImage: ImageMetadata;
}

const { title, highlight, testimonials = [] } = Astro.props;
---

<section id="references" class="-mt-40 mb-16">
  <div
    class="bg-blue-50 dark:bg-slate-800 mb-32 pointer-events-none"
    aria-hidden="true"
  >
  </div>
  <div class="relative mx-auto px-4 sm:px-6 max-w-7xl">
    <div class="py-4 sm:py-6 lg:py-8 pt-8 lg:pt-12">
      {
        (title || highlight) && (
          <div class="md:mx-auto mb-8 max-w-3xl text-center">
            {highlight && (
              <p
                class="mb-2 font-semibold text-primaryLight dark:text-blue-200 text-base uppercase tracking-wide"
                set:html={highlight}
              />
            )}
            {title && (
              <h2
                class="mb-4 font-heading font-bold text-4xl md:text-5xl leading-tighter tracking-tighter"
                set:html={title}
              />
            )}
          </div>
        )
      }
      <div class="mx-auto max-w-2xl dark:text-white">
        <div class="relative w-full" id="testimonial-carousel">
          <!-- Slides container -->
          <div class="pt-[4rem] sm:pt-[6rem] overflow-hidden">
            <div class="flex transition-transform duration-500 ease-in-out" id="testimonial-track">
              {
                testimonials.map(
                  ({ author, description, url, coverImage }, i) => (
                    <div
                      class="flex flex-col flex-shrink-0 justify-center px-2 pb-5 w-full"
                      data-testimonial-slide
                    >
                      <div class="flex flex-col items-center bg-blue-50 dark:bg-slate-800 shadow-lg hover:shadow-md mx-auto p-10 rounded-xl w-full max-w-xl">
                        <img
                          src={coverImage.src}
                          alt={coverImage.alt}
                          class="mt-[-90px] sm:mt-[-125px] border-2 border-slate-300 dark:border-slate-600 rounded-full w-[100px] sm:w-[150px] h-[100px] sm:h-[150px] object-cover"
                        />
                        <p class="my-5 dark:text-slate-200 text-lg text-center italic">
                          <span class="text-lg">&ldquo;</span>
                          {description}
                          <span class="text-lg">&rdquo;</span>
                        </p>
                        <span class="opacity-75 mr-2">-</span>
                        {author}
                        {url && (
                          <a href={url} class="opacity-50 hover:opacity-75 mt-1 text-sm italic transition-opacity">
                            {url}
                          </a>
                        )}
                      </div>
                    </div>
                  )
                )
              }
            </div>
          </div>

          <!-- Navigation buttons -->
          {testimonials.length > 1 && (
            <button
              type="button"
              id="testimonial-prev"
              class="group top-1/2 left-0 sm:-left-4 z-30 absolute flex justify-center items-center focus:outline-none -translate-y-1/2 cursor-pointer"
              aria-label="Previous testimonial"
            >
              <span
                class="inline-flex justify-center items-center bg-gray-200/70 dark:bg-gray-700/50 dark:group-hover:bg-gray-600/70 group-hover:bg-gray-300/80 shadow-sm rounded-full w-10 h-10 transition-colors duration-200"
              >
                <svg
                  class="w-4 h-4 text-gray-600 dark:text-gray-300"
                  aria-hidden="true"
                  xmlns="http://www.w3.org/2000/svg"
                  fill="none"
                  viewBox="0 0 6 10"
                >
                  <path
                    stroke="currentColor"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M5 1 1 5l4 4"></path>
                </svg>
              </span>
            </button>
            <button
              type="button"
              id="testimonial-next"
              class="group top-1/2 right-0 sm:-right-4 z-30 absolute flex justify-center items-center focus:outline-none -translate-y-1/2 cursor-pointer"
              aria-label="Next testimonial"
            >
              <span
                class="inline-flex justify-center items-center bg-gray-200/70 dark:bg-gray-700/50 dark:group-hover:bg-gray-600/70 group-hover:bg-gray-300/80 shadow-sm rounded-full w-10 h-10 transition-colors duration-200"
              >
                <svg
                  class="w-4 h-4 text-gray-600 dark:text-gray-300"
                  aria-hidden="true"
                  xmlns="http://www.w3.org/2000/svg"
                  fill="none"
                  viewBox="0 0 6 10"
                >
                  <path
                    stroke="currentColor"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="m1 9 4-4-4-4"></path>
                </svg>
              </span>
            </button>
          )}

          <!-- Dot indicators -->
          {testimonials.length > 1 && (
            <div class="flex justify-center gap-2 mt-6" id="testimonial-dots">
              {testimonials.map((_, i) => (
                <button
                  type="button"
                  class:list={[
                    'w-2.5 h-2.5 rounded-full transition-all duration-300 cursor-pointer',
                    i === 0 ? 'bg-primary dark:bg-blue-400 scale-110' : 'bg-gray-300 dark:bg-gray-600 hover:bg-gray-400 dark:hover:bg-gray-500'
                  ]}
                  data-testimonial-dot={i}
                  aria-label={`Go to testimonial ${i + 1}`}
                />
              ))}
            </div>
          )}
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  function initCarousel() {
    const track = document.getElementById('testimonial-track');
    const prevBtn = document.getElementById('testimonial-prev');
    const nextBtn = document.getElementById('testimonial-next');
    const dots = document.querySelectorAll('[data-testimonial-dot]');
    const slides = document.querySelectorAll('[data-testimonial-slide]');

    if (!track || slides.length <= 1) return;

    let current = 0;
    const total = slides.length;

    function goTo(index: number) {
      current = ((index % total) + total) % total;
      track.style.transform = `translateX(-${current * 100}%)`;
      dots.forEach((dot, i) => {
        dot.classList.toggle('bg-primary', i === current);
        dot.classList.toggle('dark:bg-blue-400', i === current);
        dot.classList.toggle('scale-110', i === current);
        dot.classList.toggle('bg-gray-300', i !== current);
        dot.classList.toggle('dark:bg-gray-600', i !== current);
      });
    }

    prevBtn?.addEventListener('click', () => goTo(current - 1));
    nextBtn?.addEventListener('click', () => goTo(current + 1));
    dots.forEach((dot) => {
      dot.addEventListener('click', () => {
        const index = Number(dot.getAttribute('data-testimonial-dot'));
        goTo(index);
      });
    });

    // Auto-advance every 6 seconds
    let interval = setInterval(() => goTo(current + 1), 6000);
    const carousel = document.getElementById('testimonial-carousel');
    carousel?.addEventListener('mouseenter', () => clearInterval(interval));
    carousel?.addEventListener('mouseleave', () => {
      interval = setInterval(() => goTo(current + 1), 6000);
    });
  }

  initCarousel();
  document.addEventListener('astro:after-swap', initCarousel);
</script>
